{% extends 'base.html.twig' %}

{% block title %}Cuenta{% endblock %}

{% block body %}
    <h1>Cuenta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cuentum.id }}</td>
            </tr>
            <tr>
                <th>Saldo</th>
                <td>{{ cuentum.saldo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cuenta_index') }}">back to list</a>

    <a href="{{ path('app_cuenta_edit', {'id': cuentum.id}) }}">edit</a>

    {{ include('cuenta/_delete_form.html.twig') }}
{% endblock %}
